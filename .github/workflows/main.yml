name: unusedbutexample
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: get
      run: |
        curl https://xshss.sse.codesandbox.io/lyrics?artist=disturbed&song=stricken
